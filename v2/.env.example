# Sports2 API base URL (no trailing slash)
# Dev with proxy: use /api/v1 so Vite proxies to backend (avoids CORS)
# Direct/Docker: use http://localhost:5000/api/v1 (ensure backend allows CORS)
VITE_API_URL=/api/v1

# Optional: backend URL when using proxy (default: http://localhost:5000)
# VITE_PROXY_TARGET=http://localhost:5000
